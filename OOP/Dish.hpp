#include <iostream>
#include <vector>

using std::string; using std::vector;

class Dish {
	string name;
	vector<string> ingredients;
	unsigned price;
	vector<string> catogries;
	string cousine;

public:
};
